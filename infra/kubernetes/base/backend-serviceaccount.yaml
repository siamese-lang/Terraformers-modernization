apiVersion: v1
kind: ServiceAccount
metadata:
  name: terraformers-backend
  labels:
    app.kubernetes.io/name: terraformers-backend
    app.kubernetes.io/part-of: terraformers-modernization

# Add the IRSA role annotation in an environment-specific overlay.
# Do not commit account-specific IAM role ARNs to the public base manifest.
