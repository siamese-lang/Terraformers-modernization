# Test Resources

This directory is reserved for public-safe backend test fixtures.

Rules:

- Do not store real AWS responses that include account ids or ARNs.
- Do not store real JWTs, access tokens, or credentials.
- Use synthetic fixtures only.
