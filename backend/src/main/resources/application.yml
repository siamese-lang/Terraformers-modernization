spring:
  application:
    name: terraformers-backend-modernization
  profiles:
    default: local

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

terraformers:
  runtime:
    required-env: []
